<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="samsoya.cameroonapp">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".SettingsActivity"></activity>
        <activity android:name=".ConfirmAudioActivity" />
        <activity android:name=".RecordAudioActivity" />
        <activity
            android:name=".ConfirmVideoActivity"
            android:label="@string/title_activity_confirm_video"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GameActivity"
            android:label="@string/title_activity_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".DocumentSelectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TakePhotoActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_take_photo"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".ConfirmPhotoActivity"
            android:label="@string/title_activity_confirm_photo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DocumentUploadedConfirmationActivity"
            android:label="@string/title_activity_document_uploaded_confirmation"
            android:theme="@style/AppTheme.NoActionBar" />

        <receiver
            android:name=".ChooserReceiver"
            android:exported="false" />
    </application>

</manifest>